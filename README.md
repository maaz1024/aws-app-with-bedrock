# aws-app-with-bedrock
This is a full-stack, serverless web application that generates blog posts using Generative AI. A user enters a topic on a simple web interface (built with Streamlit), and the AWS backend (using API Gateway, Lambda, and Bedrock) writes a complete article and saves it to an Amazon S3 bucket.
